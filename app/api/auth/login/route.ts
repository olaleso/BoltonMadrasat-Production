import { authenticate, body, fail, required, sha256 } from "@/lib/backend";
import { sql } from "@/db";
export async function POST(request: Request) { try { const input = await body(request); const user = await authenticate(required(input.email, "Email"), required(input.password, "Password")); const token = crypto.randomUUID() + crypto.randomUUID(); await sql`insert into sessions(user_id,token_hash,expires_at) values(${user.id},${await sha256(token)},now()+interval '12 hours')`; return new Response(JSON.stringify({ ok: true, user: { id:user.id,email:user.email,displayName:user.displayName,role:user.role } }), { status:200, headers:{ "content-type":"application/json", "set-cookie":`madrasat_session=${token}; HttpOnly; SameSite=Lax; Path=/; Max-Age=43200${process.env.NODE_ENV === "production" ? "; Secure" : ""}` } }); } catch (error) { return fail(error); } }
